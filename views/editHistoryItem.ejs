<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Item - <%= listname %></title>
</head>
<body>
    <h2>Edit Item in <%= listname %></h2>

    <form action="/history/edititem/update" method="POST">
        <input type="hidden" name="id" value="<%= itemToEdit.id %>">
        <input type="hidden" name="listname" value="<%= listname %>">

        <label>Item Name</label>
        <input type="text" name="itemname" value="<%= itemToEdit.itemname %>" required><br>

        <label>Quantity</label>
        <input type="number" name="quantity" value="<%= itemToEdit.quantity %>" required><br>

        <label>Price</label>
        <input type="number" step="0.01" name="price" value="<%= itemToEdit.price %>" required><br>

        <button type="submit">Update Item</button>
    </form>

    <form action="/history/data" method="POST" style="margin-top: 10px;">
        <input type="hidden" name="listing" value="<%= listname %>">
        <button type="submit">Cancel</button>
    </form>
</body>
</html>
