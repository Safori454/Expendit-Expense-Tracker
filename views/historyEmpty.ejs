<%- include('partials/header', { title: 'Empty list' }) %>

<div class="page-head">
  <div>
    <h1 class="h1"><%= listname %></h1>
    <p class="muted">This list has no items yet.</p>
  </div>
  <div class="page-actions">
    <a class="btn btn-ghost" href="/home">Dashboard</a>
    <a class="btn btn-primary" href="/list?listname=<%= encodeURIComponent(listname) %>">Open list</a>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <div class="alert">Add your first item inside the list page.</div>
  </div>
</div>

<%- include('partials/footer') %>
